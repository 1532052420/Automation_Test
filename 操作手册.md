# AutomationTest 自动化测试框架 · 操作手册

> 项目位置：`~/Desktop/AutomationTest`（macOS x64）
> 框架来源：https://github.com/1532052420/AutomationTest-1
> 本机环境配置完成日期：2026-09-09

---

## 0. 环境总览（本机已配置）

| 组件 | 版本 | 位置/说明 |
|---|---|---|
| Python（venv） | 3.8.20 | `./.venv`（pyenv 编译安装于 `~/.pyenv/versions/3.8.20`） |
| pytest | 5.0.0 | venv 内 |
| selenium | 3.141.0 | venv 内 |
| Appium-Python-Client | 0.48 | venv 内（与 Appium 1.x 服务配套） |
| Pillow | 8.4.0 | venv 内（原 5.3.0 无法在 py3.8 编译，放宽） |
| JPype1 | 1.2.1 | venv 内（原 0.7.5 与 py3.8.20 不兼容，放宽） |
| allure-pytest / allure | 2.7.0 / 2.46.1 | venv / brew（`/Users/ouyang/homebrew/bin/allure`） |
| JDK | 8 (Temurin 1.8.0_504) | `~/Library/Java/JavaVirtualMachines/jdk8u504-b01`（另有 JDK17 并存） |
| Maven | 3.9.16 | brew + `~/.local/maven` 双份，配阿里云镜像 `~/.m2/settings.xml` |
| Appium 服务 | 1.22.3 | 项目内 `./node_modules`（自带 uiautomator2 驱动 v2.8.0），端口 4726 |
| Android SDK | platforms;android-29 + build-tools;29.0.3 | `~/Library/Android/sdk`（已补全） |
| adb | 1.0.41 | PATH 可用 |

**为什么是 Python 3.8 而不是系统的 3.14？** 框架 requirements 钉的是 2018~2021 年的旧版本（pytest 5.0、selenium 3.141、Appium-Client 0.48），这些在 3.14 上无法安装，必须用 3.8 独立环境。

---

## 1. 用到的技术框架

| 框架 | 作用 |
|---|---|
| **pytest** | 测试用例收集/执行/断言的基础框架，配合 xdist 并行、rerunfailures 失败重跑 |
| **requests** | HTTP 接口请求（API 自动化核心） |
| **selenium** | Web UI 自动化，驱动 Chrome/Firefox（本框架走 selenium 3.141 + Grid 方式） |
| **Appium + Appium-Python-Client** | 移动端（Android/iOS）UI 自动化；本机配 **Appium 1.22.3** + **uiautomator2 驱动 v2.8.0** |
| **Pillow** | 截图/图像处理（App 元素截图、RGB 提取） |
| **JPype1 + tess4j(Java)** | 通过 JVM 桥接调用 Java 的 Tesseract 做**验证码识别** |
| **allure-pytest + allure CLI** | 测试报告生成（`generate_*_report.py` 汇总） |
| **locust** | 性能/压测（未启用，按需装） |
| **Maven + JDK8 + tess4j/dubbo** | 框架启动时用 `mvn dependency:copy-dependencies` 拉取 Java 依赖 jar（56 个），用于 dubbo 接口调用、验证码识别 |
| **pycryptodome / Js2Py / kazoo / zkclient** | 加密、JS 执行、Zookeeper/dubbo 相关 |

---

## 2. 框架能实现什么功能

1. **API 接口自动化**：HTTP 请求、断言（PyHamcrest）、环境切换、失败重跑、allure 报告
2. **Web UI 自动化**：Selenium Grid 多浏览器执行、元素操作、截图
3. **APP UI 自动化**：Android（UiAutomator2）/iOS 真机/模拟器，支持多设备并行、录屏、验证码识别
4. **性能测试**：locust 脚本（`cases/performance/`，未启用）
5. **Dubbo 接口调用**（泛化调用，Java jar 已就绪）
6. **数据库操作**：Oracle(cx_Oracle)/MySQL(PyMySQL)/Redis/PostgreSQL 等客户端已备
8. **验证码识别**：tess4j 图像识别（需放入字库到 `common/java/lib/tess4j/tessdata/`）

---

## 3. 目录结构（核心）

```
AutomationTest/
├── run.sh                       ← 【统一执行入口】(本机新增)
├── run_api_test.py              ← API 执行入口
├── run_app_ui_test.py           ← APP 执行入口
├── run_web_ui_test.py           ← Web 执行入口
├── run_app_ui_monkey_test.py    ← APP Monkey 测试
├── generate_*_test_report.py    ← 报告生成入口
├── requirements.txt             ← Python 依赖清单（严格版本）
├── .venv/                       ← Python 3.8 虚拟环境
├── node_modules/                ← Appium 1.22.3（含 uiautomator2 驱动）
├── base/                        ← 客户端封装（api/web_ui/app_ui 各平台 client）
├── cases/                       ← ★测试用例目录（api/web_ui/app_ui/performance）
├── common/                      ← 公共工具（httpclient/selenium/appium/hamcrest/fileTool 等）
│   └── java/lib/java/libs/      ← maven 拉取的 56 个 jar
├── config/                      ← ★全部配置文件
├── init/                        ← 初始化（api/web_ui/app_ui/java/httpserver）
├── page_objects/                ← 页面对象（元素定位）
├── pojo/                        ← 数据模型
├── packages/android/            ← 自带 demo APK（墨迹天气）
├── test_data/                   ← 测试数据
└── logs/ output/                ← 日志 / 测试报告数据
```

---

## 4. 环境配置明细（装了什么、用什么命令、去哪找）

### 4.1 系统工具（Homebrew，前缀 `/Users/ouyang/homebrew`）
| 装了什么 | 命令 | 产物位置 |
|---|---|---|
| pyenv | `brew install pyenv` | `~/homebrew/bin/pyenv` |
| Maven | `brew install maven`（另下载独立版到 `~/.local/maven`） | `~/homebrew/bin/mvn` |
| Allure | `brew install allure` | `~/homebrew/bin/allure` |
| JDK8 | 免 sudo 下载 Temurin tar.gz 解压（cask 需 sudo 不可用） | `~/Library/Java/JavaVirtualMachines/jdk8u504-b01` |
| Python 3.8.20 | `pyenv install 3.8.20` | `~/.pyenv/versions/3.8.20` |
| Android commandline-tools | 手动下载解压 | `~/Library/Android/sdk/cmdline-tools/latest` |
| Android platform/build-tools | `sdkmanager --licenses && sdkmanager "platforms;android-29" "build-tools;29.0.3"` | `~/Library/Android/sdk/` |
| Appium 1.22.3 | 项目内 `npm install appium@1.22.3`（并替换 uiautomator2-server 为 v2.8.0） | `./node_modules/` |

### 4.2 Python 依赖（venv 内）
```bash
cd ~/Desktop/AutomationTest
/Users/ouyang/.pyenv/versions/3.8.20/bin/python3.8 -m venv .venv
.venv/bin/pip install -U pip "setuptools==68.0.0" wheel
.venv/bin/pip install -r requirements.txt
# 兼容修复（详见 FAQ）：
.venv/bin/pip install "Pillow==8.4.0" "JPype1==1.2.1" "markupsafe==1.1.1" "bcrypt==4.0.1"
.venv/bin/pip uninstall -y typeguard
```
依赖核对清单见本文档第 0 节；`requirements.txt` 31 项中 27 项严格按版本，4 项例外（Pillow/JPype1 编译限制放宽、scikit-image/opencv/locust 为死代码按需补装）。

### 4.3 Java 依赖（jar）
```bash
cd ~/Desktop/AutomationTest
export JAVA_HOME=$(/usr/libexec/java_home -v 1.8)
mvn -U -f config/java/pom.xml dependency:copy-dependencies -DoutputDirectory=common/java/lib/java/libs
# 或直接跑用例时由 init/java/java_maven_init.py 自动执行
```
产物：`common/java/lib/java/libs/`（56 个 jar：tess4j/dubbo/curator/fastjson/gson…）

### 4.4 环境变量（`~/.zshrc` 已配置）
```bash
export ANDROID_HOME="$HOME/Library/Android/sdk"
export ANDROID_SDK_ROOT="$ANDROID_HOME"
export PATH="$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools:$PATH"
# JAVA_HOME 建议在 run.sh 中按 JDK8 设置（全局保留 JDK17 给其他工具）
```
> 注意：全局 `java` 仍是 JDK17；跑本框架时 `run.sh` 会自动把 `JAVA_HOME` 切到 JDK8。

### 4.5 Maven 镜像
`~/.m2/settings.xml` 已配置阿里云镜像（加速中央仓库下载）。

---

## 5. 配置文件都在哪、配什么

| 文件 | 作用 |
|---|---|
| `config/pytest.ini` | pytest 配置（运行脚本自动生成） |
| `config/report.conf` | 报告路径/模板 |
| `config/api_demoProject_test.conf`（在 `config/demoProject/`） | API demo 项目服务器地址（默认 www.baidu.com，可改成你的被测服务） |
| `config/demoProject/app_ui_android_devices_info_demoProject.conf` | ★APP 设备信息：udid、platformVersion、appium server 地址端口、被测 app 包名/Activity、chromeDriver、systemport 等 |
| `config/app_ui_config.conf` | 设备池大小 |
| `config/httpserver.conf` | 内置 http.server 地址/端口 |
| `config/web_ui_config.conf` | selenium hub 地址（默认指向内网 10.130.72.85:4444，需改成本地 hub 才能本地跑） |
| `config/java/pom.xml` | Java 依赖（tess4j/dubbo 等） |
| `config/demoProject/api_demoProject_test.conf` | API demo 环境 |

**改设备配置时**：`udids` 用 `adb devices` 查询；`platformVersion` 用 `adb shell getprop ro.build.version.release`；`appPackages/appActivitys` 用 `adb shell dumpsys window | grep mCurrentFocus` 查看前台应用。

---

## 6. 执行入口

### 6.1 统一入口（本机新增 `run.sh`）
```bash
cd ~/Desktop/AutomationTest
./run.sh status          # Appium 是否在跑
./run.sh start-appium    # 启动 Appium(4726)
./run.sh api             # 跑 API 用例
./run.sh app             # 跑 APP(Android) 用例
./run.sh app -d cases/app_ui/android/demoProject/   # 跑指定目录 APP 用例
./run.sh report api      # 生成 API 报告
```
`run.sh` 自动设置 JAVA_HOME(JDK8)/PATH/ANDROID_HOME，无需手动 export。

### 6.2 原始入口（跳过 run.sh 时）
```bash
export JAVA_HOME=$(/usr/libexec/java_home -v 1.8)
export PATH="$PWD/.venv/bin:/Users/ouyang/homebrew/bin:$PATH"
export ANDROID_HOME=$HOME/Library/Android/sdk

.venv/bin/python run_api_test.py                 # API
.venv/bin/python run_app_ui_test.py -tt phone \
  -dif config/demoProject/app_ui_android_devices_info_demoProject.conf   # APP
.venv/bin/python run_web_ui_test.py              # Web
.venv/bin/python generate_api_test_report.py     # 报告
```

### 6.3 常用参数
| 参数 | 说明 |
|---|---|
| `-d <dir>` | 指定用例目录（默认 cases/api/ 或 cases/app_ui/） |
| `-k <kw>` | 只跑匹配关键字的用例 |
| `-m <mark>` | 按 mark 表达式筛选 |
| `-r <n>` | 失败重跑次数 |
| `-clr 1` | 清空历史 allure 数据 |
| `-e test/release` | API 环境（写入 config/tmp/env.json） |
| `-tt phone/windows` | APP 测试类型（必填） |
| `-dif <file>` | 多设备信息文件（phone 时必填） |

---

## 7. APP 自动化专题

### 7.1 前置准备（本机已完成）
- Appium 服务：`./run.sh start-appium`（Appium 3.7.0，端口 4726，唯一版本）
- 真机连接：`adb devices` 确认设备在线
- uiautomator2 驱动：server 包已装到设备（`io.appium.uiautomator2.server`）

### 7.2 执行
```bash
cd ~/Desktop/AutomationTest
./run.sh app -d cases/app_ui/android/demoProject/
```
执行时手机会自动打开被测 App 并执行点击/搜索等操作。

### 7.3 ⚠️ 华为手机（EMUI）安装拦截
手机开了"纯净模式/应用未备案"风险拦截，**adb 安装每个 apk 都要在手机上手动确认**：
`继续安装 → 了解风险 → 勾选"已了解此应用存在风险" → 仍然安装`（若弹华为账号登录页直接返回）。
建议在手机 `设置 → 系统与更新 → 纯净模式` 关闭，之后不再拦截。
> 本机还差 `io.appium.uiautomator2.server.test` 一个包没装（被拦截），补装命令：
> ```bash
> adb install -r ~/Desktop/AutomationTest/node_modules/appium/node_modules/appium-uiautomator2-server/apks/appium-uiautomator2-server-debug-androidTest.apk
> ```

### 7.4 元素定位工具（用什么）
| 场景 | 工具 | 用法 |
|---|---|---|
| Android 元素定位 | **Appium Inspector**（未装，需 `brew install --cask appium-inspector`） | 连 `http://127.0.0.1:4726/wd/hub`，可看控件树、resource-id、坐标，生成定位表达式 |
| Android 轻量定位 | `adb shell uiautomator dump` | 导出当前界面 XML，查 text/resource-id/bounds |
| Android 命令行 | `adb shell dumpsys window` | 查前台 Activity/包名 |
| Web 定位 | Chrome DevTools（F12） | 查 id/class/xpath |
| 框架内定位写法 | `page_objects/app_ui/android/demoProject/pages/startPage.py` | 参考现有 page object 的写法（By.id / By.xpath / 坐标） |

元素定位器页面（`./run.sh locator`）：采用与测试平台一致的**左侧深色边栏布局**——返回平台、品牌版本、「📂 快速打开」（用例/元素/页面操作三个下拉）、🔄 刷新按钮、底部「📱 设备状态」都在侧栏，右侧为截图/元素/教学三栏工作区。"设备截图"栏标题旁有**显示尺寸下拉**——默认（铺满栏宽）/ iPhone 15 Pro（393×852）/ iQOO 15（411×905），机型预设居中带手机壳质感显示；点选映射按渲染比例自动适配，选择存 localStorage 刷新不丢。两个页面右下角都有日/夜模式切换悬浮按钮；窄屏（≤920）三栏自动纵向堆叠。

### 7.5 自动录屏与失败证据（allure）
APP UI 用例执行时**自动录屏**，失败自动挂证据，业务用例零侵入（由 `cases/app_ui/conftest.py` 驱动，实现在 `common/video_evidence/`）：

- **录屏后端自动选择**：设备有 `screenrecord` 二进制（主流 Android）走 adb 录屏（单次上限 180s）；没有的机型（如华为 HarmonyOS）自动走 `screencap` 抽帧循环兜底（约 5fps，帧率取决于设备 screencap 速度）
- **断言成功**：报告里只有断言截图（`assert_true_with_shot` 挂载，现状不变），临时录屏自动清理
- **断言失败**：按失败时刻 T 生成 4 件证据挂入 allure：
  | 附件 | 内容 |
  |---|---|
  | `failure_video` (mp4) | 裁剪 [T-5s, T+5s] 的失败现场视频，报告内可直接播放 |
  | `failure_screenshot` (png) | 失败瞬间截图（先于延迟收尾抓取，现场最及时） |
  | `failure_reason` (txt) | pytest 失败原因堆栈 |
  | `recording_log` (txt) | 录屏全过程日志（起止时刻、抽帧索引、裁剪参数），兜底后端含逐帧抓取时间表 |
- **重跑(`-r`)**：每次失败尝试各自生成一套证据，落在各自尝试的 allure 结果里
- **开关与参数（环境变量）**：`RECORDING_ENABLED`(默认 true)、`RECORDING_REQUIRED`(录屏失败是否终止用例，默认 false 只告警)、`RECORDING_KEEP_ON_SUCCESS`(成功保留原始录屏，默认 false)、`RECORDING_BEFORE_SECONDS`/`RECORDING_AFTER_SECONDS`(默认 5/5)、`ADB_SERIAL`(多设备时一般自动从用例设备信息读取，无需设置)
- **单条用例关闭录屏**：用例上加 `@pytest.mark.no_recording`
- 产物落盘：`output/video_evidence/`（tmp 已随 .gitignore 忽略）；录屏任何异常只记日志，不影响用例真实结果

已知限制：`screenrecord` 单条用例超 180s 录制截断（超长用例失败时以完整原始录屏兜底挂载）；静态画面下设备端编码器尾部帧稀疏，证据视频可能略短于 10s（失败时刻及后续现场完整）；同一设备不要并行跑多个执行进程（会互相干扰录屏）。

### 7.6 本轮实机适配记录（2026-09-12，OPPO Android 16）

### 7.7 自己改 Web 页面布局（元素定位器/测试平台，免费工具方案）

工具就两样，都免费、零配置：
1. **VS Code**（https://code.visualstudio.com 下载安装即用）：写 HTML/CSS/JS/Python，自带语法高亮；本框架网页是零构建的纯静态文件，不需要 Node/npm
2. **浏览器 F12 开发者工具**（Chrome/Edge 自带，不用装）：改网页布局的主力工作流——
   - F12 → 点左上角箭头图标 → 在页面上点中要改的元素 → 右侧 Styles 面板实时勾选/修改 CSS 看效果
   - 满意后把样式抄进 `element_locator/static/style.css`（平台在 `web_platform/static/`），Cmd+R 刷新确认
   - 布局结构在 `index.html`，交互逻辑在 `index.js`；响应式写法参考 style.css 末尾的 `@media` 媒体查询

启动方式：`./run.sh platform`（8080，元素定位器已并入平台 `/locator` 页面，不再有独立 8001 服务），改的是静态文件，刷新浏览器即生效，无需重启服务。- **Appium 3 可用性检查修复**：`run_app_ui_test.py` 原先按 Appium 1.x 顶层 `status==0` 判断，Appium 2+/3+ W3C 返回 `value.ready`，误判"server不可用"；已兼容两种协议
- **报告脚本 macOS 兼容修复**：`generate_app_ui_test_report.py` 原用 Linux 语法 `netstat -anp`（macOS 无 `-p`）导致崩溃；已改为查不到残留进程时跳过
- **demo 登录流程增强**（`demoToolLoginPage`）：
  - teardown 的 `reset_app()` 在 Appium 3 下等价 `pm clear`，**每次跑完清应用数据**，首启隐私协议/通知权限弹窗每次都会出现 → 页对象新增 `deal_first_launch_dialogs()`（无弹窗快速跳过）
  - Appium 输入文本时切换输入法，vivo 键盘会弹「键盘选择」布局面板遮挡登录按钮 → 页对象新增 `dismiss_ime_panel()`（键盘/面板存在才按 BACK，防误触返回导航）
  - 断言统一升级 `assert_true_with_shot`：断言成功即截图入 allure（登录弹层、协议 toast、登录成功三处均有成功截图）
- **元素等待超时异常精简**（`appOperator.getElement/getElements`）：selenium 原生 `TimeoutException` 会把 Appium 服务端 JS 堆栈整段拼进异常消息，allure 步骤里全是噪声；现统一精简为「等待元素超时(Ns)：[locator] 元素未找到」，失败步骤只保留定位器与等待时长
- **日志分行美化**：`appOperator` 的点击/输入/坐标/断言日志改为多行步骤块——`▶ 步骤N · 动作` + 缩进详情行（定位/坐标/输入内容）+ `✔ 成功 · 耗时` 或 `✖ 失败 · 原因`；`cases/app_ui/conftest.py` 在每条用例前后输出开始/结束横幅并重置步骤编号，allure 的 log 附件即可当步骤时间线阅读

---

## 8. Web UI 专题

Web UI 走 Selenium Grid：`config/web_ui_config.conf` 的 `selenium_hub` 默认指向内网 `10.130.72.85:4444`。要本机跑需：
1. 下载 selenium-server-standalone-3.141.0.jar，`java -jar selenium-server-standalone-3.141.0.jar -port 4444`
2. 下载 chromedriver（匹配本机 Chrome 版本）放入 PATH
3. 把 `selenium_hub` 改为 `http://127.0.0.1:4444/wd/hub`
（本次按范围未配置，需要时再处理）

---

## 9. 报告

测试结果写入 `output/`（`api/app_ui/web_ui` 各自的 `report_data/`），用 allure 查看：
```bash
allure serve output/api/report_data/
# 或 ./run.sh report api 生成静态报告
```

APP UI 报告（`./run.sh report app`）生成时自动注入附件缩略图补丁：步骤附件默认显示小缩略图（图片为缩略图、视频为首帧画面，上限 200x120），点击缩略图用 allure 原生查看器放大/播放，查看器媒体自适应窗口高度（修复手机长图/视频按原始像素渲染、半屏只能看到一条的问题）。补丁由 `generate_app_ui_test_report.py` 注入到生成的报告 index.html，幂等可重复生成。

---

## 10. 常见问题 FAQ

**Q1 为什么系统 Python 3.14 不能用？**
框架钉的旧依赖（pytest5/selenium3.141/Appium-Client0.48/Pillow5.3 等）在 3.14 无 wheel 且编不过。必须用 3.8 venv。

**Q2 typeguard 报错 `AssertionError` 导致 pytest 起不来？**
sqlacodegen→inflect→typeguard4 的 pytest 插件与 pytest5 冲突。已处理：venv 内 `setuptools` 固定 68.0.0、`pip uninstall typeguard`。重装依赖后请保持这两条。


**Q4 JPype1 0.7.5 import 报错？**
与 py3.8.20 不兼容，已换 1.2.1。

**Q5 端口 4723 被占？**
全局 Appium 3.7 占用 4723，本框架用 4726（`config/.../app_ui_android_devices_info_demoProject.conf` 的 `server_ports`）。

**Q6 Appium 会话失败 / uiautomator2 instrumentation 崩溃？**
已把项目内 uiautomator2-server 从 v4.27.0 换成 v2.8.0（v4 的 test apk 在 Android10 上缺 AndroidJUnit4 类）。备份在 `/tmp/bak_*.apk`。

**Q7 每次跑 API 都要等 maven 重新下载 jar？**
`init/java/java_maven_init.py` 每次执行 `mvn -U` 重拷依赖（约 30s~1min，走本地 ~/.m2 缓存，不重新下载）。

---

## 11. 已验证记录（2026-09-09）

- ✅ API 链路端到端跑通：maven 初始化 → pytest 收集/执行 → allure 报告生成
- ✅ Appium 1.22.3 服务正常（`/wd/hub/status` 返回 status 0）
- ⚠️ APP 用例执行待补装 `io.appium.uiautomator2.server.test` 包后复测（7.3 节操作）
