#!/usr/bin/env bash
# ============================================================
# 自动化测试平台（macOS 双击运行）
# 双击 → 自动启动 Web 执行平台并打开浏览器页面，无需选择
# ============================================================
cd "$(dirname "$0")" || exit 1
exec ./run.sh platform
