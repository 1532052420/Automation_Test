# 接口自动化接入测试平台 · 设计方案

> 调研对象：`web_platform/`（Flask 执行平台，APP UI 自动化专用）+ `run_api_test.py`（接口自动化入口）
> 结论：**可以接入，改动集中在一个执行器分支 + 一个新页面 + 一处导航项**，无需新写执行器。
> 本文件为设计方案，落地时按第七章分期执行。

> **实施状态（v6.2 · 2026-09-17 14:00:29）**：一期已落地并端到端验证通过。
> - 后端：`runtime_config`（环境扫描 / conf 解析 / 标记解析 / 用例树）、`runner`（`kind` 分支）、`routes`（`/api-test` + 三个 `/api/api_test/*`）✅
> - 证据链：`doRequest.HTTP_TRACE` 请求响应留痕 + `cases/api/conftest.py` 失败挂载 + `report_data` 文本证据渲染 ✅
> - 前端：新增接口测试页；全站按 Apple 官网设计语言重做（顶部毛玻璃导航 / SF 字体栈 / 品牌蓝胶囊按钮 / 18px 圆角卡片），默认主题改为日间 ✅
> - 实测：启动接口任务 → 实时日志 → 失败证据（`failure_reason` + `http_trace`）→ Allure 报告生成与服务就绪，全链路通过
> - 未做（按需再排）：第六章的团队协作（发起人字段已落，排队与按人筛选未做）、四期并发、`--lf` 重跑上次失败
> - 章节号说明：下文原「第六章 团队协作」「第七章 分期落地」保持不变；本行所述"一期"对应第七章表格中的一期范围。

---

## 一、可行性判断：为什么能低成本接入

判断「能不能接」不看框架有多大，看**两边有几处真正的耦合点**。逐项比对后只有三处需要对齐，且都已在平台侧存在：

| 耦合维度 | 平台现状（APP UI） | 接口框架现状 | 是否一致 |
|---|---|---|---|
| 执行内核 | `subprocess` 起 `pytest -c config/pytest.ini` | `run_api_test.py` 最后就是 `pytest.main([...])` | ✅ 同一个内核 |
| 结果产物 | `--alluredir output/runs/<run_id>/allure-results` | `--alluredir output/api/report_data/` | ✅ 同为 allure-pytest 的 `*-result.json` |
| 任务模型 | run_id + 状态机 + 日志增量 + 停止 + 归档 | 无（只有一次性命令行执行） | ✅ 直接复用平台的 |
| 报告生成 | `allure generate` + `http.server` 静态服务 | `generate_api_test_report.py` 同样跑 `allure generate` | ✅ 平台侧实现更完善 |
| **前置校验** | Appium 探测 / adb 在线 / 包名已安装 / 写设备临时文件 | **完全不需要** | ❌ **唯一真正的差异** |

**结论**：`ExecutionManager` 里 90% 的代码（run_id 生成、进程组管理、日志流式收集、进度正则统计、看门狗超时、SIGTERM→SIGKILL、`result.json` 归档、Allure 报告生成与静态服务）对接口测试**完全通用**。唯一要动的是 `_start_run_locked()` 里那四段 App UI 专属的前置校验。

---

## 二、三种接入方案对比

| 方案 | 做法 | 改动量 | 评价 |
|---|---|---|---|
| **A. 复用 ExecutionManager，加 `kind` 分支** | 在 `runner.py` 按 `kind` 走不同前置校验与 pytest 参数 | 后端 ~80 行 | ✅ **推荐**：一处改动，详情页/报告页/附件服务/停止/归档全部白拿 |
| B. 独立 `ApiExecutionManager` | 复制一份 `runner.py` 改 | ~400 行重复 | ❌ 违反 DRY；后续修 bug（如日志统计正则）要改两处 |
| C. 只做「命令行封装」 | 页面点按钮起 `run_api_test.py` shell | 小 | ❌ 拿不到结构化状态/进度/停止/报告，退化成命令面板 |

**推荐方案 A**。理由不是「省事」，而是：接口与 APP UI 的**任务语义完全同构**（都是「跑一批 pytest 用例、收日志、出 allure 报告」），差异只在「怎么准备环境」。用分支表达差异，比用两个类表达差异更贴合实际。

---

## 三、方案 A 的改动清单

### 3.1 后端（6 个文件）

| 文件 | 改动 | 关键点 |
|---|---|---|
| `web_platform/runtime_config.py` | ① `scan_case_tree()` 加参数 `root_dir`（默认 `cases/app_ui`）<br>② 新增 `list_api_env_confs()`：正则扫 `config/*/api_*_(test\|release).conf`<br>③ 新增 `parse_api_conf(path)`：读 `[servers]url` 与 `[isInit]init`<br>④ 新增 `list_markers()`：解析 `config/pytest.conf` 的 `markers=` 段，供标记下拉多选 | 全部复用现有 `ConfigParser` 写法，不引新依赖 |
| `web_platform/runner.py` | ① `start_run(kind='app_ui', owner='', ...)`<br>② `_start_run_locked` 把 Appium/adb/包名/设备临时文件四段收进 `if kind == 'app_ui':`<br>③ pytest 参数按 kind 拼：接口侧追加 `-e <env>` 并先写 `config/tmp/env.json`<br>④ `_public_task` 增加 `kind` / `env` / `owner` 字段（`owner` 供团队场景区分发起人） | **核心改动**，约 90 行 |
| `web_platform/routes.py` | 新增 `GET /api-test`（页面）、`GET /api/api_test/options`（环境 conf + 用例树 + 默认值）、`POST /api/api_test/run`（校验后转 `runner.manager.start_run`） | `/api/run/<id>`、`/api/run/<id>/log`、`/stop`、`/report/open`、`/api/runs` 全部复用 |
| `web_platform/report_data.py` | `_pick_images()` → `_pick_attachments()`：图片照旧保留，**额外放行 `text/plain`**（接口的请求/响应留痕） | 约 10 行；不改则接口详情页看不到任何证据 |
| `web_platform/admin_routes.py` | `_UPLOAD_TARGETS` 增加 `'api_cases': cases/api`，`_NAME_RULES` 复用 `^test_[A-Za-z0-9_]+\.py$` | 让「用例管理」能上传接口用例（`py_compile` 校验现成） |
| `cases/api/conftest.py` | **新增文件**：`pytest_runtest_makereport` 钩子在失败时把 `HttpResponseResult` 的 status / body / headers 作为 text 附件挂入 allure | 补齐接口侧证据链，否则平台详情页无料可看 |

### 3.2 前端（2 个文件 + 1 个新模板）

| 文件 | 改动 |
|---|---|
| `templates/api_test.html` | **新增**。骨架照抄 `run.html`（执行配置/选择用例/执行中三段），`<style>` 照抄 `perf.html` 的主题变量写法 |
| `static/app.js` | ① `renderSidebar()` 的 `items` 数组插入 `['/api-test', '🔌', '接口测试']`<br>② 新增 `initApiTest()`（约 120 行，结构对齐 `initPerf()` + `initRun()`）<br>③ 页面分发追加 `else if (page === 'api-test') initApiTest();`<br>④ 新增「我是谁」输入框（存 `localStorage['platform_user']`，随启动请求带给后端）；勾选状态同样存 `localStorage` 按人隔离 |
| `static/style.css` | **基本不动**。`.card / .field / .fields / .tree / .logbox / .meta / .bar / .status / .btn` 全部复用 |

> 前端整体是「模板 + 全局 `app.js` + `data-page` 分发」的约定，新模块只要遵守这个约定，就不需要碰构建、路由表或样式体系。

---

## 四、UI 设计

### 4.1 信息架构

侧边栏（`renderSidebar` 的 items 数组）当前 6 项，接口测试插在第 5 位：

```
首页 → 执行用例 → 测试报告 → 用例管理 → 【接口测试】 → 元素定位器 → 性能压测
```

放在「用例管理」之后、「元素定位器」之前：左边是「准备用例」，右边是「元素/性能」这类专项工具，接口测试与「执行用例」同属执行域，紧邻更符合心智。

### 4.2 页面分区（自上而下 3 张卡片 + 1 个折叠区）

> **设计原则：按「用户会变的意图」排列，不按「命令行参数」排列。** 凡是从 conf 推导出来的、或从来不动的，一律不上表单。

**① 运行环境**
| 控件 | 说明 |
|---|---|
| 环境下拉 | 选项来自 `list_api_env_confs()` 扫描结果，**选项文案自带地址**：`test · http://your-service:8080`，不硬编码 |
| 连通性徽标 | 环境选中后自动探测一次：绿点「可达 42ms」/ 红点「不可达」 |
| 执行前初始化测试数据（复选） | 对应 conf 的 `[isInit] init`，默认按 conf 显示，本次可覆盖 |

**② 选择用例（cases/api）**
- 三级树：`项目 / 模块 → 文件 → 文件::类::方法`
- 顶部**搜索框**：输入即过滤（匹配文件 / 类 / 方法名），配「全选搜索结果」按钮
- 工具条：全选 / 全不选 / **只选上次失败**（读上次接口 run 的失败节点）
- 右上角实时显示「已选 N 条」

**③ 执行中**（结构 100% 复用 `run.html` 的执行区）
- Run ID / 状态徽章 / 共-过-失-异常 / 进度条 / 实时日志框 / 停止执行 / 查看详情
- 日志框额外高亮两类行：`GET/POST <url> -> <status>`（请求行）与 `FAILED` 行

**④ 高级（默认折叠）**
- 失败重跑次数 `--reruns`（默认 0）
- 任务超时（分钟，默认 30，覆盖 `RUN_TIMEOUT_SECONDS`）

### 4.3 字段取舍：砍掉了什么、为什么

第一版设计把 `run_api_test.py` 的命令行参数**逐个翻译成表单字段**，这是「按实现排列」而不是「按意图排列」，有 4 处站不住脚：

| 原设计字段 | 处置 | 理由 |
|---|---|---|
| 目标服务地址（只读输入框） | **删除**，合并进环境下拉的选项文案 | 它不是一个「值」，而是「选环境」的**结果**。单独占一行输入框（还是只读的）纯属噪音。放进 option 文案（`test · http://xxx`）既保留防呆价值，又不占版面 |
| 关键字 `-k` | **删除**，改为用例树顶部搜索框 | 用例树已经能精确勾到方法级，`-k` 是重复能力；而且 `-k` 是 pytest 的语法，让使用者记命令行参数名本身就是设计失败。搜索框语义直白、所见即所得，还能配「全选搜索结果」 |
| 标记 `-m`（自由文本） | **保留，但降级为下拉多选** | 这是唯一「树结构表达不了」的维度——标记是**跨文件**的（`search_kw`、`no_recording` 散落在不同文件），必须留。但选项可从 `config/pytest.conf` 的 `markers=` 自动解析，用户不该手打 |
| 清空历史 Allure 数据 `-clr` | **删除** | 这是**运维动作**，不该和执行参数并列；且语义危险（会清掉上一次的 allure 数据）。平台已有 `/api/runs/clear`「清空全部记录」入口，职责归那里 |

结果：**字段从 8 个降到 5 个，卡片从 4 张降到 3 张（+1 折叠区）**。留在页面上的每个字段都能回答一个问题：「什么情况下用户会改它？」

- 运行环境 —— 换环境跑（每次验收都会用）
- 执行前初始化数据 —— 数据被污染时（偶发）
- 用例搜索框 —— 只想跑某一块（常用）
- 失败重跑次数 —— 排查偶发抖动（偶发）
- 任务超时 —— 接口挂起时（罕见，故折叠）

### 4.4 与 APP UI 执行页的差异（刻意做的减法）

| 维度 | APP UI 执行页 | 接口测试页 | 原因 |
|---|---|---|---|
| 设备/Appium 状态区（`devgrid`） | 有 | **去掉** | 接口测试不依赖设备 |
| 录屏配置卡片 | 有 | **去掉** | 接口无屏幕可录 |
| 运行环境选择 | 无 | **新增** | `test/release` 是接口侧的核心变量 |
| 用例树根目录 | `cases/app_ui` | `cases/api` | — |
| 连通性指示 | Appium 可用性 | 被测服务可达性 | 语义等价替换 |

### 4.5 详情页 / 报告页的复用策略

| 页面 | 复用方式 |
|---|---|
| `/runs/<run_id>` 详情页 | **直接复用**。仅把「设备 / App」两列在 `kind == 'api'` 时替换为「环境」一列 |
| `/api/run/<run_id>/cases` | 直接复用（`report_data.list_run_cases` 解析 allure，与语言无关） |
| `/api/runs/<run_id>/res/<source>` | 复用，但需配合 `report_data` 放行 text 附件 |
| `/api/run/<run_id>/report/open` | 直接复用（allure 静态报告 + 端口服务 + 同 run 服务复用逻辑） |
| 首页「执行总览」 | 复用，统计卡片增加「类型」筛选（APP UI / 接口 / 全部） |

---

## 五、平台操作流程设计

### 5.1 主流程（6 步）

```
① 准备用例      本地写 → 放 cases/api/<项目>/api/ ；或用「用例管理」上传 .py（自动 py_compile 校验）
② 打开接口测试页  自动加载：环境列表 + 用例树（含标记选项）
③ 选运行环境     test / release → init 开关自动带出，目标地址直接显示在选项文案里
④ 勾选用例       搜索框过滤 + 树勾选（搜索框等价于原来的 -k，但不暴露 pytest 语法）
⑤ 调执行参数     一般不用动；需要时展开「高级」改重跑次数 / 超时
⑥ 开始执行       平台校验 → 创建 run → 起 pytest 子进程 → 实时日志滚动 → 归档
⑦ 查看结果       通过：归档，可生成 Allure 报告在线看
                 失败：详情页看用例明细 + 请求/响应留痕 → 修用例 → 回 ④
```

### 5.2 执行前置校验（在 `_start_run_locked` 的接口分支里）

| # | 校验 | 失败提示 |
|---|---|---|
| 1 | 至少勾选一条用例 | 「请至少选择一个用例」 |
| 2 | 环境 conf 存在且可解析出非空 url | 「配置 <conf> 解析不到服务地址」 |
| 3 | 被测服务可达（可选，仅警告不拦截） | 「服务不可达，仍可执行（用例会失败）」 |
| 4 | 无并发任务（复用现有串行锁） | 「已有任务 <run_id> 正在运行，请等待完成或先停止」 |

### 5.3 状态机（完全复用）

```
PENDING ──▶ RUNNING ──┬──▶ PASSED    （exit_code == 0）
                      ├──▶ FAILED    （exit_code != 0）
                      ├──▶ STOPPED   （人工点停止）
                      └──▶ ERROR     （看门狗超时 / 启动失败 / 平台重启中断）
```

### 5.4 关键交互规则

1. **串行执行**：与 APP UI **共用同一把锁**（`ExecutionManager._start_lock`），同一时刻只允许 1 个任务。理由：两者都会写 `config/tmp/` 下的全局文件，并发会互相污染。
2. **冲突可见 + 可排队**：有任务运行时，「开始执行」变为「排队等待」，页面顶部常驻「当前占用」条显示 `<run_id> · 发起人 · 已跑 N/M · 已运行 X 分 Y 秒`（详见 6.4 决策二）。
3. **切环境不重置选择**：切换 test/release 只刷新地址与 init 开关，已勾选用例保持不动（避免误操作丢失选择）。
4. **报告按需生成**：不自动跑 `allure generate`（省时间）；点「查看报告」时若未生成则先生成 —— 平台现有逻辑已如此，无需改。
5. **失败重跑与「只选上次失败」配合**：先 `--reruns 1` 排除偶发抖动，仍失败则用「只选上次失败」二次定位。
6. **执行总览混排**：`output/runs/` 目录共用，run_id 连续编号；列表加「类型」列 + 筛选器，默认显示全部。

### 5.5 与命令行入口的关系

接入后**命令行入口保留、不废弃**：

| 场景 | 用哪个 |
|---|---|
| 本地调试单个用例、看详细 stdout | `./run.sh api -k login -s 1` |
| 团队执行、留痕、看报告、查历史 | 平台「接口测试」页 |
| CI 流水线 | 命令行（平台是人工操作用的） |

两者共用同一份 `config/`、`cases/`、`pytest.ini`，**不存在两套配置**——这是接入设计里最重要的一条约束。

---

## 六、团队协作流程设计

### 6.1 先认清一件事：这套框架的并发能力是「1」

平台当前是**单机、串行、无账号**的形态。这不是偷懒，是框架的硬约束决定的：

| 约束 | 出处 | 后果 |
|---|---|---|
| 全局环境文件 | `config/tmp/env.json`（接口）/ `config/app_ui_tmp/<pid>`（APP） | 两个任务同时跑会互相串环境 |
| 单设备 | `_start_run_locked` 只取 conf 里第一台设备 | APP UI 物理上无法并行 |
| 单把锁 | `ExecutionManager._start_lock` + `running_task()` | 同一时刻只允许 1 个任务 |
| 无身份 | `_public_task` 没有 owner 字段 | 分不清谁跑的 |

所以**团队用平台的第一个设计决策不是「加权限」，而是「把排队做显式」**。

### 6.2 三个角色

| 角色 | 谁 | 职责 | 主要界面 |
|---|---|---|---|
| **用例作者** | QA / 开发 | 写用例、走 git review、保证用例可维护 | 本地 IDE + 「用例管理」上传验证 |
| **执行者** | QA | 选环境、勾用例、执行、看留痕、提缺陷 | 接口测试页 + 详情页 |
| **观察者** | PM / 开发 / 负责人 | 看结果，不执行 | 执行总览 + run 链接（只读） |

### 6.3 协作流程

```
① 用例作者  写接口用例 → 提交 git → PR review → 合并
                                    ↓ 用例就绪
② 执行者    打开平台（首次填「我是谁」）→ 接口测试页 → 选环境（看清地址）→ 勾用例
③ 执行者    点「开始执行」；若有人在跑 → 看到「当前占用」条 → 点排队等待
④ 执行中    平台实时日志；观察者可在「执行总览」看到进行中的任务
⑤ 执行者    结束后生成报告 → 把 run 链接发给相关人（观察者点开即看，无需环境）
⑥ 判定      失败 → 详情页看请求/响应留痕
              · 用例问题 → 回 ① 修用例
              · 接口问题 → 提缺陷，附 run 链接（缺陷单里带证据，不用复述）
```

### 6.4 五个设计决策与理由

**决策一：平台部署成「团队共用一台」，而不是「每人本地一份」**

理由：平台的价值就在于**共享执行历史与报告**。每人本地一份 = 各自看各自的结果，等于退化成「命令行 + 各自的 allure」，平台白建。
代价是执行要排队——这个代价必须付，但可以通过「显式排队」把它变得可预期。

**决策二：把「拒绝」改成「排队」**

现状：已有任务运行时 `start_run` 直接返回「已有任务正在运行」，第二个人被拒且看不到任何上下文。
改为：页面顶部常驻「当前占用」条 —— `<run_id> · 发起人 · 已跑 3/12 · 已运行 4 分 12 秒`，并把「开始执行」变成「排队等待」。

理由：串行不可绕过，但「不可用」和「要等 3 分钟」是两种完全不同的体验。团队场景下，**可预期比快更重要**。

**决策三：必须加「发起人」字段（最低成本、最高收益）**

本机 / 局域网模式下**不做账号体系**（YAGNI），只在页面顶部放一个「我是谁」输入框，存 `localStorage`，随 run 一起落进 `result.json`。

理由：多人共用一个平台，历史列表里如果分不清谁跑的、失败该找谁，平台就从「协作工具」退化成「一堆无主的记录」。一个字段解决，不需要任何权限体系。
二期接真账号时该字段平移即可（`admin_routes` 里已有同样的迁移注释：`output/admin_meta.json` → 未来的 `file_manage` 表）。

**决策四：用例变更走 Git，平台不做在线编辑器**

平台「用例管理」保留**上传**能力（用于快速验证单个改动），但**不做在线编辑**。

理由：
- 用例是代码。多人同时改代码的协作问题（冲突 / 回退 / 追责）应该用代码工具解决，不要在平台里重造一个弱版。
- 上传同名文件会互相覆盖（虽有历史备份），只适合「单人快速验证」，不适合日常协作。
- 平台的职责边界是**执行 + 留痕 + 看报告**，编辑不是它的活。

配套：上传的文件要在列表里显示**上传人**（平台已有 `output/admin_meta.json` 存这个字段，正好用上）。

**决策五：共享历史，不共享「待执行选择」**

- 执行历史、报告、附件 → **全员可见**（团队资产）
- 「这次勾了哪些用例」→ 存 `localStorage`，**按人隔离**（个人临时意图）

理由：如果勾选状态是全局的，A 勾了一半去开会、B 上来直接点执行，就会跑出一批 A 想跑但 B 不知情的用例。**执行意图必须私有，执行结果必须公开。**

### 6.5 环境可见性代替权限

谁能跑 `release` 是个管理问题，本机模式下不做权限。但要在 UI 上**显著标出当前环境**（环境下拉选项文案自带地址，已在 4.2 落地）。

理由：误在 release 上跑造数用例的代价，远高于「多看一眼」；而「多看一眼」的成本是零。**用可见性代替权限**是这一阶段最划算的做法。

### 6.6 未来的并发路径（值得提前预留）

如果团队规模上去、排队成为瓶颈，最划算的一步是：**把 `config/tmp/env.json` 改成按 run_id 传参**（pytest 自定义 option 或环境变量），接口任务即可并行——因为接口测试**不占用设备**，唯一的瓶颈就是这个全局文件。

也就是说，接口测试接入不只是「加个页面」，它是**平台从「单设备串行」走向「部分并发（接口并行 + APP 串行）」的契机**。这一点在实现 `kind` 分支时就要预留：env 不要写死成全局文件，留一个「按 run 传参」的口子。

---

## 七、分期落地建议

| 期 | 内容 | 交付标准 |
|---|---|---|
| **一期（最小可用）** | `runner.py` 加 `kind` 分支 + `api_test.html` + 导航项 + `admin_routes` 支持 `api_cases` + **「我是谁」发起人字段**（落 `result.json`） | 能在平台勾用例、跑起来、看实时日志、生成并在线查看 Allure 报告；历史里分得清谁跑的 |
| **二期（证据链）** | `cases/api/conftest.py` + `report_data` 放行 text 附件 + 详情页展示请求/响应 | 接口失败时能在平台内看到请求 URL、状态码、响应体，不用翻日志 |
| **三期（团队体验）** | 「当前占用」条 + 排队等待、执行总览按**类型 / 发起人**筛选、「只选上次失败」、接口用例模板一键生成（对齐 `element_locator/case_generator.py` 的思路） | 多人共用时执行可预期、结果可按人检索，平台内可完成「新建接口用例」全流程 |
| **四期（可选·并发）** | `config/tmp/env.json` 改为按 run_id 传参 → 接口任务并行（APP UI 仍串行） | 排队不再是瓶颈；接口压测/回归可与其他任务同时跑 |

---

## 八、风险与取舍

| # | 风险 | 说明 | 对策 |
|---|---|---|---|
| 1 | 执行总览混排 | 接口与 APP 任务共用 `output/runs/`，统计会混在一起 | 加 `kind` 字段 + 前端类型筛选；若坚持物理隔离，则改 `output/api_runs/`（需同步改 `runner` / `report_data` / `routes` 三处，成本约 1 小时） |
| 2 | 平台主环境是 Python 3.8 | 接口用例若用 3.9+ 语法（如 `dict \| dict`）会直接报错 | 上传时用 `py_compile` 预检（`admin_routes` 已有该能力，复用即可） |
| 3 | 超时语义不同 | APP 侧 30 分钟合理，接口侧可能 5 分钟就够 | 超时做成页面可配，默认 30 分钟 |
| 4 | `config/tmp/env.json` 是全局文件 | 多任务并发会串环境 | 现有串行锁已规避；若未来要并发，需把 env 改成按 run_id 传参（pytest `--env` 自定义 option 或环境变量） |
| 5 | 真实接口未就绪 | 当前 conf 指向 `www.baidu.com`，示例用例无法验证真实业务链路 | 一期先接一个真实服务的 conf 做验收，不要拿 demo 配置验收 |
| 6 | 平台标题与定位 | 平台自称「AppUI 自动化测试平台」（`app.py` 注释、侧边栏 brand、`/api/status` 的 `service: 'app-ui-platform'`） | 接入接口测试后应改为「自动化测试平台」，涉及 3 处文案 + 1 处服务标识 |

---

## 九、一句话总结

接口自动化接入平台的本质**不是「移植一套框架」，而是「给已有的执行器加一个环境准备分支」**：执行内核、任务模型、日志/停止/归档、Allure 报告、附件服务这五套机制平台已经全部具备且与语言无关，真正要新写的只有一个页面、一个 `kind` 分支和一份接口侧的 conftest 证据钩子。

---

*方案生成时间：2026-09-17 13:08*
