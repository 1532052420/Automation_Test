# AutomationTest 框架 · 接口自动化实现调研报告

> 调研对象：`/Users/ouyang/Desktop/AutomationTest`（基于开源项目 1532052420/AutomationTest 二次开发）
> 调研范围：框架整体结构与运行机制 → 接口自动化的实现方式 → PO 模式的应用 → 接口用例的存放与组织 → 用例调用模式与执行流程 → 可落地实现思路
> 说明：本报告为只读调研，除新增本文件外未改动任何框架代码。

---

## 一、框架整体结构与运行机制

### 1.1 分层模型（四层 + 两侧支撑）

框架按「**执行入口 → 初始化 → 客户端封装 → 用例**」四层纵向组织，配置与数据模型作为横向支撑：

| 层 | 目录 | 职责 | 关键文件 |
|---|---|---|---|
| 执行入口层 | 根目录 | 参数解析、环境准备、拼装 pytest 参数并拉起 pytest | `run_api_test.py`、`run_app_ui_test.py`、`run.sh` |
| 初始化层 | `init/` | 测试前置数据构造（按平台分 `api/` `app_ui/` `httpserver/` `java/`） | `init/api/api_init.py`、`init/api/demoProject/demoProjectInit.py` |
| 客户端封装层 | `base/` | 每个被测项目一个「客户端」，聚合配置 + 请求器 + DB 客户端 | `base/api/demoProject/api_demoProject_client.py` |
| 用例层 | `cases/` | 只写业务断言，不写底层细节 | `cases/api/demoProject/api/test_demoProject_login.py` |
| 配置链（横向） | `config/` + `pojo/` | `.conf` 文件 → `pojo` 数据对象 → `base/read_*.py` 读取器 | `config/demoProject/api_demoProject_test.conf`、`pojo/api/demoProject/demoProjectConfig.py` |
| 公共能力（横向） | `common/` | HTTP、断言、Appium、文件、时间、Java 桥等通用工具 | `common/httpclient/doRequest.py`、`common/hamcrest/hamcrest.py` |

### 1.2 三条关键机制

**机制一：单例 + 懒初始化的「三层配置读取链」**

```
config/*.conf  ──ConfigParser──▶  pojo/*.py（纯数据对象）  ──▶  base/read_*.py（读取器，单例）
```

所有读取器都用同一套写法（`__new__` 单例 + `__inited` 守卫），保证**一个进程内配置只读一次**：

```python
class API_DemoProject_Read_Config(object):
    __instance=None; __inited=None
    def __new__(cls,*a,**kw):
        if cls.__instance is None: cls.__instance=object.__new__(cls)
        return cls.__instance
    def __init__(self,config_file_path=None,env=None):
        if self.__inited is None:
            ...  # 只在首次进入时真正读文件
            self.__inited=True
```

**机制二：pytest 作为唯一执行内核**

框架不自己写调度器，所有用例执行最终都落到 `pytest.main()`。执行入口只做「参数翻译」：把命令行参数翻译成 pytest 参数列表，再用 `-c config/pytest.ini` 指定配置文件。

**机制三：跨进程上下文用「临时文件」传递，而非内存**

这是本框架一个很关键的设计取舍：多设备并行时每个设备跑在独立子进程里，父进程与子进程之间不共享内存，于是用 `config/app_ui_tmp/<pid>` 这类文件传递设备信息；API 侧则用 `config/tmp/env.json` 传递环境（`test` / `release`）。

---

## 二、接口自动化测试的具体实现方式

### 2.1 核心组件

| 组件 | 文件 | 作用 |
|---|---|---|
| HTTP 请求器 | `common/httpclient/doRequest.py` | 基于 `requests.Session` 的薄封装，统一超时/代理/重试/连接池 |
| 响应对象 | `pojo/httpResponseResult.py` | 4 个字段：`status_code` / `body` / `cookies` / `headers` |
| 项目客户端 | `base/api/demoProject/api_demoProject_client.py` | 聚合「配置 + DB 客户端 + 请求器」，用例的入口门面 |
| 环境配置 | `config/demoProject/api_demoProject_test.conf` | `[servers] url=...`、`[isInit] init=1` |
| 断言库 | `common/hamcrest/hamcrest.py` | 对 PyHamcrest 的链式包装 `assert_that(x).is_equal_to(y)` |

### 2.2 DoRequest 的实现要点

`DoRequest` 不是裸 `requests` 调用，它做了四件对自动化很关键的事：

1. **Session 复用 + 连接池**：`HTTPAdapter(pool_connections=10, pool_maxsize=10, max_retries=2)`，并同时挂到 `http://` 与 `https://`。
2. **状态在实例上累积**：`_headers` / `_cookies` / `_proxies` / `_timeout` / `_verify` 都是实例属性，支持 `setXxx` / `updateXxx` 增量修改——**登录后的 token 可以直接 `updateHeaders()` 注入，后续请求自动带上**。
3. **Cookie 自动回收**：每次响应后 `_dealResponseResult()` 会执行 `self.updateCookies(self._session.cookies.get_dict())`，把服务端下发的 Cookie 同步到本地字典，同时序列化进 `HttpResponseResult.cookies`（`ujson.dumps`）。
4. **统一出口封装**：所有方法（`get` / `post_with_form` / `post_with_file` / `put` / `delete` / `getFile`）都不返回 `requests.Response`，而是返回自定义的 `HttpResponseResult`——**框架内部不泄漏第三方对象**。

```python
def _dealResponseResult(self, r):
    r.encoding = self._encoding                       # 固定 utf-8，规避中文乱码
    httpResponseResult = HttpResponseResult()
    httpResponseResult.status_code = r.status_code
    httpResponseResult.headers = r.headers.__str__()
    self.updateCookies(self._session.cookies.get_dict())
    httpResponseResult.cookies = ujson.dumps(self.getCookies())
    httpResponseResult.body = r.content.decode(self._encoding)   # body 是 str，不是 bytes
    return httpResponseResult
```

> 注意：`post_with_form` 用的是 `data=params`（表单），JSON 提交需自行 `ujson.dumps` 并配合 `**kwargs` 传 `json=`。

### 2.3 项目客户端的实现要点

```python
class API_DemoProject_Client(object):
    def __new__(cls,*a,**kw):            # 单例
        ...
    def __init__(self,config_file_path:str=None,env:str=None):
        if self.__inited is None:
            self.demoProjectConfig   = API_DemoProject_Read_Config(config_file_path, env).config
            self.demoProjectDBClients= API_DemoProject_DB_Clients()
            self.doRequest           = DoRequest(self.demoProjectConfig.url)
            self.__inited = True
```

客户端 = **一个被测系统的门面（Facade）**，把「服务器地址、初始化开关、DB 连接、HTTP 请求器」一次性装好，用例里只需 `self.client.doRequest.get(path)`。

### 2.4 环境切换（`-e test|release`）的实现

这是接口测试里唯一一处「跨进程传参」，链路如下：

```
run_api_test.py -e release
   └─▶ FileTool.writeObjectIntoFile({'env':'release'}, 'config/tmp/env.json')   # 入口先落盘
         └─▶ API_DemoProject_Read_Config.__init__(env=None)                     # 用例侧不传 env
               └─▶ 读 config/tmp/env.json → env='release'
                     └─▶ config_file_path='config/demoProject/api_demoProject_release.conf'
```

优先级：**显式 `config_file_path` > 显式 `env` 参数 > `config/tmp/env.json` > 默认 `test`**。

### 2.5 断言机制

`common/hamcrest/hamcrest.py` 把 PyHamcrest 包成链式 API，用例读起来接近自然语言：

```python
assert_that(200).is_equal_to(httpResponse.status_code)
assert_that(body).contains('admin')
```

- 类 `__Assert` 也是单例，通过 `assert_that(value)` 返回实例并写入 `self.value` 再链式调用；
- 除 PyHamcrest 原生匹配器外，`custom_matchers.py` 自定义了 `h_is_true()` / `h_is_empty()`；
- `common/assertTool.py` 提供补充断言：正则匹配 `isRegularMatch`、文件内容相等 `isFilesEqual`、文件大小相等 `isFilesSizeEqual`。

> 历史版本曾用 `assertpy`（见 git 记录中已删除的旧用例），当前已统一为 `common/hamcrest`。

---

## 三、PO（Page Object）模式在本框架中的应用

本框架的 PO 模式**不是单一层次，而是「元素对象 + 页面对象」双层，再叠一个「操作引擎」**。这是理解本框架的关键。

### 3.1 三层结构

```
page_objects/
├── createElement.py                     ← 元素工厂（类方法 create）
├── app_ui/
│   ├── locator_type.py                  ← 定位方式枚举（Locator_Type）
│   └── wait_type.py                     ← 显式等待类型枚举（Wait_Type）
└── app_ui/android/demoProject/
    ├── elements/kuaigeLoginElements.py  ← ① 元素层：只声明「元素是什么」
    └── pages/kuaigeLoginPage.py         ← ② 页面层：只声明「在页面上做什么」

common/appium/appOperator.py             ← ③ 操作引擎：真正与 Appium 交互（65KB 大文件）
pojo/elementInfo.py                      ← 元素的 DTO（locator_type/value + wait_* 等 7 个字段）
```

### 3.2 元素层：`CreateElement` 工厂 + `ElementInfo` DTO

```python
# page_objects/createElement.py
@classmethod
def create(cls, locator_type, locator_value, expected_value=None,
           wait_type=None, wait_expected_value=None, wait_seconds=30, desc=None):
    elementInfo = ElementInfo()
    elementInfo.locator_type = locator_type
    elementInfo.locator_value = locator_value
    ...
    return elementInfo
```

```python
# page_objects/app_ui/android/demoProject/elements/kuaigeLoginElements.py
class KuaigeLoginElements:
    def __init__(self):
        self.et_phone = CreateElement.create(Locator_Type.ID, 'com.recordlife.kuaige:id/etPhone',
                                             wait_type=Wait_By.VISIBILITY_OF)
        self.btn_login = CreateElement.create(Locator_Type.ID, 'com.recordlife.kuaige:id/btnLogin',
                                              wait_type=Wait_By.VISIBILITY_OF)
```

**要点**：元素层是**纯数据声明**，不含任何操作逻辑。定位值（id/xpath）、定位方式、显式等待类型、等待超时全部收敛在这里。`desc` 字段由 GUI 定位器「真机抓取」自动注入，仅作备注透传。

### 3.3 页面层：只写业务动作，不写定位值

```python
class KuaigeLoginPage:
    def __init__(self, appOperator):
        self.appOperator = appOperator
        self._elements = KuaigeLoginElements()      # 组合元素层

    def click_login(self):
        self.appOperator.click(self._elements.btn_login)     # 引用元素名，不埋定位值

    def assert_login_success(self):
        ok = self.appOperator.is_toast_visible('登录成功', wait_seconds=5)
        if not ok:
            try:
                self.appOperator.getElement(self._elements.text_login_success)
                ok = True
            except Exception:
                pass
        self.appOperator.assert_true_with_shot('断言登录成功', ok, '未捕获到「登录成功」toast 或页面文案')
```

**「元素与用例分离」**是本框架 PO 的硬约束（`element_locator/case_generator.py` 文件头注释明确写了这一条）：页面方法只引用 `self._elements.<name>`，绝不出现 `'com.recordlife.kuaige:id/etPhone'` 这类字符串。定位值一变，只改元素文件一处。

### 3.4 操作引擎：`AppOperator`

页面层不直接碰 `webdriver`，而是通过 `AppOperator`（`common/appium/appOperator.py`）间接操作。它承担：

- 元素查找 + 显式等待（`wait_type` / `wait_seconds` 由元素对象带入）；
- 点击/输入/滑动/长按/Toast 判定/截图等原子操作；
- **断言即存证**：`assert_true_with_shot()` 断言失败时自动截图并挂入 allure；
- 通过 `DoRequest` 直连 Appium Hub 的 `/session/<id>/...` 接口完成 `webdriver` 未覆盖的能力（地理位置、飞行模式、context 切换等）。

### 3.5 PO 模式在「接口侧」的对应物

接口测试没有页面，但本框架保持了**同构的分层思想**，可以一一映射：

| APP UI 侧 | 接口侧 | 说明 |
|---|---|---|
| `page_objects/**/elements/*Elements.py` | `config/demoProject/api_*.conf` | 变化的「地址/定位值」外置 |
| `page_objects/**/pages/*Page.py` | `base/api/**/api_*_client.py` | 业务动作封装（客户端门面） |
| `common/appium/appOperator.py` | `common/httpclient/doRequest.py` | 底层操作引擎 |
| `pojo/elementInfo.py` | `pojo/httpResponseResult.py` | 传递数据的 DTO |
| 用例只调 `page.xxx()` | 用例只调 `client.doRequest.xxx()` | 用例层保持薄 |

**结论**：本框架的 PO 是「广义 PO」——凡是「把易变的定位/地址/协议细节从用例中抽离，用例只表达业务意图」的分层，都归入同一套模式。接口侧的 `client + config + doRequest` 就是 PO 在接口域的等价实现。

---

## 四、接口测试用例的存放位置与组织方式

### 4.1 目录约定

```
cases/
├── __init__.py
├── api/                                       ← 接口用例根目录（run_api_test.py 默认扫描目标）
│   ├── __init__.py
│   └── demoProject/                           ← 项目名（每个被测项目一个目录）
│       ├── __init__.py
│       └── api/                               ← 按「用例类型」再分一层
│           ├── __init__.py
│           └── test_demoProject_login.py      ← 用例文件
└── app_ui/                                    ← APP UI 用例（另有 conftest.py 驱动录屏）
```

README「编码规范」中明确的约定（当前仓库保留）：

- 每个测试项目在 `cases/` 下建一个目录，目录内含 `api`、`scenrarios`（原文如此拼写）两个子目录；
- **`api/` 放单接口用例，`scenrarios/` 放多接口串联的业务场景用例**（历史提交中存在 `cases/api/demoProject/scenrarios/__init__.py`）；
- 测试模块文件命名：`test_projectName_moduleName.py`；
- 测试类以 `Test` 开头，用例方法以 `test_` 开头；
- case 对应的 setup/teardown fixture 统一命名 `fixture_[test_case_method_name]`；
- 用例文件按业务分层时**允许再建子目录**（历史提交中曾有 `cases/api/demoProject/api/auth/test_demoProject_login.py`，即按模块 `auth` 再分一层）。

### 4.2 用例文件的编写模板

```python
#-*- coding:utf8 -*-
from common.hamcrest.hamcrest import assert_that
from base.api.demoProject.api_demoProject_client import API_DemoProject_Client
import pytest

class TestLogin:

    def setup_class(self):
        self._api_demoProject_client = API_DemoProject_Client()
        self._login_path = '/horizon/auth/login/'

    def test_get_index(self):
        httpResponse = self._api_demoProject_client.doRequest.get(self._login_path)
        assert_that(200).is_equal_to(httpResponse.status_code)

    @pytest.mark.search_kw
    def test_search_kw(self):
        params = {'wd': 'apitest'}
        httpResponse = self._api_demoProject_client.doRequest.get(self._login_path, params)
        assert_that(200).is_equal_to(httpResponse.status_code)
```

要点：

- **`setup_class` 只做一件事**：实例化客户端、声明接口路径常量；
- 路径常量 `_login_path` 写在类里，不写在方法里——便于统一维护；
- 断言统一走 `assert_that(...)` 链式 API；
- 用例方法内不出现 URL、超时、Cookie 等细节。

### 4.3 用例分组与筛选手段

框架提供三档筛选，均落在 `pytest` 原生能力上：

| 手段 | 参数 | 作用范围 |
|---|---|---|
| 目录 | `-d cases/api/demoProject/` | 按目录批量 |
| 关键字 | `-k login` | 匹配**文件名、类名、方法名** |
| 标记 | `-m search_kw` | 按 `@pytest.mark.xxx` 自定义标记 |

标记需在 `config/pytest.conf` 的 `markers=` 中登记（该文件由 `common/pytest.py::deal_pytest_ini_file()` 按平台编码复制为 `config/pytest.ini`）：

```ini
markers=
    search_kw: 搜索关键字(demoProject的marker)
    no_recording: 关闭该条用例的自动录屏证据
```

### 4.4 当前仓库的实际情况（重要）

- **接口用例仅有一条示例**：`cases/api/demoProject/api/test_demoProject_login.py`（2 个用例），被测地址在 `config/demoProject/api_demoProject_test.conf` 中指向 `http://www.baidu.com`，属于「跑通链路」的演示配置，**尚未接入真实业务接口**；
- `cases/demoProject/api/` 下只有 `__pycache__`（`test_admin_upload.py` / `test_admin_backup.py` 的 `.pyc`），这是**测试平台自身**的接口测试用例（见 `web_platform/tests/test_platform_api.py`），运行时动态创建、跑完删除，**不是框架的接口用例**；
- `test_data/` 目录在 README 中有描述，但**当前仓库不存在**——参数化数据目前直接写在用例内（如历史版本中的 `fail_data=[('11111','1111'),('admin',''),...]`）。

---

## 五、用例调用模式与执行流程

### 5.1 完整调用链路

```
① python run_api_test.py -e test -k login -r 1
        │
        ├─ argparse 解析参数（clr/dir/env/keyword/lf/markexpr/capture/reruns）
        │
        ├─ deal_pytest_ini_file()        common/pytest.py
        │     └─ config/pytest.conf  ──按平台编码──▶  config/pytest.ini
        │
        ├─ java_maven_init()             init/java/java_maven_init.py
        │     └─ 拉取/校验 common/java/lib/java/libs 下的 jar（tess4j 等）
        │
        ├─ api_init()                    init/api/api_init.py
        │     └─ DemoProjectInit().init()
        │           ├─ 读 API_DemoProject_Read_Config().config.init
        │           ├─ init == 0 → 直接 return（不构造数据）
        │           └─ init != 0 → _deinit() 清上次构造的数据 → 构造本次数据
        │
        ├─ env 参数落盘：config/tmp/env.json = {"env": "test"}
        │     （先 truncateDir('config/tmp/') 清空历史）
        │
        └─ pytest.main(['-c','config/pytest.ini','-v',
                        '--alluredir','output/api/report_data/',
                        '-k','login','--reruns','1','cases/api/'])
                │
                ├─ pytest 收集 cases/api/ 下 test_*.py 的 Test* 类中 test_* 方法
                │
                └─ 逐条用例执行
                      ├─ setup_class()
                      │     └─ API_DemoProject_Client()            ← 单例
                      │           ├─ API_DemoProject_Read_Config(config_file_path=None, env=None)
                      │           │     └─ env=None → 读 config/tmp/env.json → 'test'
                      │           │           → config/demoProject/api_demoProject_test.conf
                      │           │           → DemoProjectConfig(url=..., init=...)
                      │           ├─ API_DemoProject_DB_Clients()  ← DB 客户端（预留）
                      │           └─ DoRequest(url)                ← Session + 连接池
                      │
                      ├─ test_xxx()
                      │     ├─ self._api_demoProject_client.doRequest.get(path, params)
                      │     │     └─ requests.Session.get → _dealResponseResult()
                      │     │           └─ HttpResponseResult(status_code/body/cookies/headers)
                      │     └─ assert_that(200).is_equal_to(httpResponse.status_code)
                      │           └─ PyHamcrest assert_that → 失败抛 AssertionError
                      │
                      └─ teardown_class()
                      │
                      └─ 结果写入 output/api/report_data/*.json（allure 原始数据）

② python generate_api_test_report.py -p 2001
        └─ allure generate output/api/report_data -o output/api/report/api_report_<时间戳>
              └─ nohup allure open -p 2001 ...  → http://<本机IP>:2001
```

### 5.2 与 APP UI 执行的差异（对照理解）

| 维度 | 接口测试 | APP UI 测试 |
|---|---|---|
| 入口 | `run_api_test.py` | `run_app_ui_test.py -tt phone -dif <设备conf>` |
| 并发模型 | 单进程 `pytest.main` | `Custom_Pool` 进程池，**一设备一进程** |
| 上下文传递 | `config/tmp/env.json` | `config/app_ui_tmp/<pid>`、`<pid>_current_desired_capabilities` |
| 设备前置 | 无 | 校验 Appium `/wd/hub/status`、清理历史 session |
| 报告目录 | `output/api/report_data/` | `output/app_ui/<设备>/<包名>/report_data/` |
| conftest | **无** | `cases/app_ui/conftest.py`：用例级自动录屏 + 失败证据四件套（截图/视频/失败原因/录屏日志）挂 allure |
| 报告端口 | `report.conf` → `api_port=2001` | `app_ui_start_port=3001` 起递增 |

> 接口侧目前**没有 conftest.py**。这意味着接口用例目前拿不到「失败自动截图/录屏」这类钩子能力（接口侧本也不需要录屏，但可考虑用 conftest 补「请求/响应全量留痕」）。

---

## 六、结合本框架的可落地实现思路

### 6.1 新增一个业务接口自动化项目（标准六步）

**Step 1 · 建配置**：`config/<project>/api_<project>_test.conf`（+ `_release.conf`）

```ini
[servers]
url=http://your-service:8080

[isInit]
init = 0
```

**Step 2 · 建 DTO**：`pojo/api/<project>/<project>Config.py`（照抄 `demoProjectConfig.py` 的字段风格，按需扩展 token、超时、公共 header 等）

**Step 3 · 建读取器**：`base/api/<project>/api_<project>_read_config.py`
—— 复用 `API_DemoProject_Read_Config` 的**单例 + `config_file_path > env > env.json > 默认` 优先级**写法；`env` 到文件名的映射按项目实际环境扩展。

**Step 4 · 建客户端**：`base/api/<project>/api_<project>_client.py`

```python
class API_<Project>_Client(object):
    __instance=None; __inited=None
    def __new__(cls,*a,**kw): ...
    def __init__(self,config_file_path=None,env=None):
        if self.__inited is None:
            self.config     = API_<Project>_Read_Config(config_file_path,env).config
            self.dbClients  = API_<Project>_DB_Clients()
            self.doRequest  = DoRequest(self.config.url)
            self.doRequest.updateHeaders({'Content-Type':'application/json'})
            self.__inited = True

    def login(self, username, password):
        """业务动作封装：登录并注入 token，后续请求自动携带"""
        r = self.doRequest.post_with_form('/api/login', {'username':username,'password':password})
        self.doRequest.updateHeaders({'Authorization': ujson.loads(r.body)['token']})
        return r
```

> 关键点：**把「需要状态传递」的业务动作（登录、取 token、取业务 id）封装成客户端方法**，用例层就不用关心 `updateHeaders` 的时序。

**Step 5 · 建用例**：`cases/api/<project>/api/test_<project>_<module>.py`

```python
class Test<Module>:
    def setup_class(self):
        self.client = API_<Project>_Client()
        self.path = '/api/<module>/list'

    def test_list_ok(self):
        r = self.client.doRequest.get(self.path, {'page':1,'size':10})
        assert_that(200).is_equal_to(r.status_code)

    fail_data = [('', 10), ('admin', 0)]
    @pytest.mark.parametrize('keyword,size', fail_data)
    def test_list_boundary(self, keyword, size):
        r = self.client.doRequest.get(self.path, {'keyword':keyword,'size':size})
        assert_that(200).is_equal_to(r.status_code)
```

**Step 6 · 建初始化**：`init/api/<project>/<project>Init.py`（实现 `_deinit()` 清数据 + `init()` 造数据），并在 `init/api/api_init.py` 中追加一行调用。

### 6.2 补齐当前接口侧的四个短板（按收益排序）

| 优先级 | 短板 | 落地做法 | 收益 |
|---|---|---|---|
| P0 | 缺少「请求/响应留痕」，接口失败只能看断言信息 | 新增 `cases/api/conftest.py`，用 `pytest_runtest_makereport` 钩子在失败时把 `HttpResponseResult` 的 status/body/headers 作为 text 附件挂入 allure | 失败可定位，对齐 APP UI 侧的「证据必留」约定 |
| P0 | `test_data/` 不存在，参数与数据耦合在用例里 | 建 `test_data/<project>/*.yaml` 或 `.json`，用 `FileTool.readJsonFromFile` 读取 + `@pytest.mark.parametrize` 注入 | 数据与代码解耦，可交非开发人员维护 |
| P1 | 接口用例未接入 Web 平台 | 在 `web_platform/runtime_config.py` 中新增 `scan_api_case_tree()`（照抄 `scan_case_tree()`，只把 `CASES_APP_UI_DIR` 换成 `cases/api`），并在 `web_platform/runner.py` 增加 API 执行分支（复用 `run_api_test.py` 的参数拼装逻辑） | 平台已支持 APP 用例，接口接入后一套平台统一管理 |
| P2 | 缺少请求链路的场景化用例目录 | 启用 `cases/api/<project>/scenrarios/`（README 原有约定），放「登录 → 下单 → 查订单 → 取消」这类多接口串联场景 | 单接口用例之外补齐业务闭环 |

### 6.3 接口用例设计的推荐范式

结合 `qa-engineer-agent` 的「全路径覆盖」要求，单接口用例建议按四类展开（与 `element_locator/case_generator.py` 的 `STEP_TYPES` 分类思路一致）：

1. **正向**：正常参数 → 200 + 业务 code 正确；
2. **边界**：`page=0` / `size=极大值` / 空字符串 / 超长字符串；
3. **异常**：缺必填参数 → 400；未带 token → 401；越权访问 → 403；不存在资源 → 404；
4. **特殊场景**：并发提交、幂等重复提交、特殊字符/中文/emoji、超时与重试。

配合 `@pytest.mark.parametrize` 一行数据一用例，配合 `--reruns` 排除偶发抖动。

### 6.4 必须注意的框架约束

1. **工作目录必须是项目根**：`DoRequest` 之外，所有配置读取都用相对路径（`'config/...'`、`'cases/api/'`），**从其他目录启动必然失败**。`run.sh` 已封装 `cd` 逻辑，优先用 `./run.sh api`。
2. **Python 版本**：README 要求 3.6.8，本机实际用 3.8.20（`.venv`）。新增用例不要使用 3.9+ 语法（如 `dict | dict`）。
3. **单例的双刃剑**：客户端与配置读取器都是单例，`__init__` 的参数**只有首次生效**。想切换环境，只能靠 `run_api_test.py -e` 落盘的 `env.json`，**不能在用例里 new 一个新客户端换 url**。
4. **`config/tmp/` 会被清空**：`run_api_test.py` 每次启动都 `truncateDir('config/tmp/')`，不要往里放需要持久化的文件。
5. **`config/pytest.ini` 是生成物**：由 `config/pytest.conf` 按平台编码复制而来，**改配置要改 `.conf`**，改 `.ini` 会在下次执行时被覆盖。
6. **`__init__.py` 必须补齐**：新增目录（`cases/api/<project>/api/`、`base/api/<project>/`、`pojo/api/<project>/`）都要有 `__init__.py`，否则 import 失败。
7. **allure 报告数据目录会被追加**：默认 `--alluredir output/api/report_data/` 不清理历史，需 `-clr 1` 才会 `--clean-alluredir`。

---

## 七、附：性能压测模块（接口测试的延伸能力）

平台另有 `perf_test/` 模块（Locust）与 `web_platform/perf_config.py`，可视为接口自动化的**延伸**而非替代：

- 请求定义与接口用例同构：`host` + `path`（支持 `{变量}` 占位）+ `method` + `headers` + `params` + `body` + `timeout`；
- 支持动态参数替换（如每次请求随机 `userId`）、`validate_status`（2xx 计成功）；
- 两种压测形状：`staircase`（阶梯爬坡）、`pulse`（周期性峰值）；
- 与功能接口自动化的**分工**：`cases/api/` 管「对不对」，`perf_test/` 管「快不快、稳不稳」。

---

*报告生成时间：2026-09-17 13:01*
